# yaml-language-server: $schema=https://schema.blue-build.org/modules/rpm-ostree-latest.json

type: rpm-ostree

repos:
  - https://copr.fedorainfracloud.org/coprs/atim/starship/repo/fedora-%OS_VERSION%/atim-starship-fedora-%OS_VERSION%.repo

install:
  - micro
  - starship
  - ffmpeg-free
  
remove:
  - firefox
  - firefox-langpacks
  - gnome-software