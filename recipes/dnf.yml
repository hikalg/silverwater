# yaml-language-server: $schema=https://schema.blue-build.org/modules/dnf-latest.json

type: dnf
repos:
  copr:
    - lilay/topgrade
  nonfree: 
    rpmfusion

install:
  packages:
    - topgrade
    - ffmpeg-free
    - fcitx5-mozc
    - fcitx5-unikey
    
remove:
  packages:
    - ffmpeg
